# Script to run the sctipt

# Read data sets

# Loop over data sets

for _surve in survey:
  # open data sets through S3:
  # report = (xarray object)
  # sv =  (xarray object)
  
  generate_report(report, sv, bottom, label):
